apiVersion: v1
kind: Service
metadata:
  name: frontendapp-service
  namespace: webapps
  labels:
    app: apache
    version: blue
spec:
  type: NodePort
  selector:
    app: apache
    version: blue
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30003

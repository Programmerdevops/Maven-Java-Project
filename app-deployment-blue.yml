apiVersion: apps/v1
kind: Deployment
metadata:
  name: batman-deployment
  namespace: webapps
  labels:
    app: Apache
spec:
  replicas: 2
  selector:
    matchLabels:
      app: Apaches
  template:
    metadata:
      labels:
        app: Apachh
        version: blue
    spec:
      containers:
        - name: batman-container
          image: prcdocker/superman-web:1
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "200m"
              memory: "300Mi"
            limits:
              cpu: "250m"
              memory: "350Mi"
